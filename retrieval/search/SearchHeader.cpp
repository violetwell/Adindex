#include "SearchHeader.h"

/*
TopDocs::TopDocs(size_t th, ScoreDoc* sds, size_t sdl):
	totalHits(th),
	scoreDocs(sds),
	scoreDocsLength(sdl)
{

}

TopDocs::~TopDocs()
{
	delete [] scoreDocs;
}

TopFieldDocs::TopFieldDocs(size_t th, FieldDoc* fds, size_t sdl):
	totalHits(th),
	fieldDocs(fds),
	scoreDocsLength(sdl)
{

}

TopFieldDocs::~TopFieldDocs()
{
	delete [] fieldDocs;
}
*/

